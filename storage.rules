rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Prototype rules only. Require authentication before production.
    match /reports/{reportId}/{fileName} {
      allow read, write: if true;
    }
  }
}
